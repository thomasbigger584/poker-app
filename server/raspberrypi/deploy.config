# Raspberry Pi deployment configuration (key=value pairs)
#
# WIPE_DB_DATA: if set to 1, the Postgres data volume is wiped before
#   every deployment run so the app starts with a fresh database.
#   Set to 0 to keep existing data between runs.
WIPE_DB_DATA=1
