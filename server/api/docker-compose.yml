name: 'api'
services:
  api-base:
    container_name: api
    image: com.twb.pokerapp/api
    build:
      context: .
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
